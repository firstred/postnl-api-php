coverage:
  precision: 2
  round: down
  range: "70...100"

  status:
    project:
      default:
        target: 70%
        threshold: 50%
        base: auto
    patch:
      default: off
    changes:
      default: off

comment:
  layout: "reach, diff, flags, files, footer"
  behavior: default
